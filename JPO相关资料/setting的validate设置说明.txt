郑杭 8-23 09:26:24
很久之前你不是问过那个validata的form的setting是干什么用的嘛，我今天查到要怎么配怎么用
是校验变更的值的
它后台会调用jsp
或者js文件
js方法返回ture就是允许，false就是不允许
这个js文件的在服务器端的路径要看你的注册中心，哪个注册中心就在哪个文件夹下面
js的文件名字到emxSystem这个配置文件下面看
键名是 eServiceSuite[注册中心].UIForm.ValidationFile
可以跟多个
用逗号隔开
jsp里面不能写前端的标签，就是纯js代码
可以调用API
其他的参考OOTB的代码就行
默认把新变更的值作为变量传递给方法