watchEffect 和watch 都会响应式的追逐语句。区别在于

watchEffect不需要和watch一样，指明需要监视哪些参数，它会自动追踪回调函数中使用过的参数
其次，watchEffect方法在声明之后会立即执行一次回调函数（这个和watch配置immediate类似）

watchEffect(() => {
    console.log(`当前温度: ${temperature.value}°C, 当前天气: ${weather.value}`);
});

因此watchEffect也可以用于处理某些数据的初始化