## 说明

* **`@contextmenu.prevent` 不仅能阻止浏览器默认右键菜单，还能让你完全接管右键行为，实现自定义上下文菜单（Custom Context Menu）** 。
  自定义右键的核心实现思路是这样的，在需要使用自定义的组件和页面中，给标题添加阻止浏览器默认右键菜单功能，同时将自定义的菜单设置为可见
* 注释:  contextmenu事件当用户右键点击元素触发， .prevent的作用 阻止默认的行为
